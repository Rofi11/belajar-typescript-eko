describe("While loop", () => {
  it("Should support While Loop in Typescript", () => {
    let counter: number = 0;

    while (counter < 10) {
      console.info(counter);
      counter++;
    }
  });
});
