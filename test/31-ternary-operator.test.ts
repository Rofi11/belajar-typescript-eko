describe("Ternary Operator", () => {
  it("Should support Ternary Operator in Typescript", () => {
    const value: number = 80;
    //Ternary operator
    const say = value >= 75 ? "Congratulation" : "Try Again";
    console.info(say);
  });
});
