"use strict";
describe("Data type", () => {
    it("should must declare", () => {
        const name = "Muhammad Rofi";
        const balance = 10000;
        const isVip = true;
        console.info(name);
        console.info(balance);
        console.info(isVip);
    });
});
