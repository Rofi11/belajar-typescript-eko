"use strict";
describe("Class", () => {
    it("Should support Class in Typescript", () => {
        // membuat class
        class Customer {
        }
        class Order {
        }
        // membuat object dari class dengan deklare
        const customer = new Customer();
        // membuat object dari class secara explisit
        const order = new Order();
    });
});
