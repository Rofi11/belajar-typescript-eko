describe("Intersection Types", () => {
    it("should support intersction types", () => {
        // intersection types
        const domain = {
            id: "1",
            name: "eko",
        };
        console.info(domain);
    });
});
export {};
